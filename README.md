# Configuration store

Static, versioned configuration blobs consumed by client software.
Files are named by content-derived identifiers and are individually
encrypted; the repository intentionally contains no readable data.
